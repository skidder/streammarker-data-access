box: golang
build:
  # The steps that will be executed on build
  steps:
    - skidder/glide-install@1.0.1
  
    - script:
        name: go build
        code: |
          go build

    - script:
        name: go test
        code: |
          make test
